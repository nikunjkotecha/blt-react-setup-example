project:
  machine_name: react_demo
  prefix: (RD)
  local:
    protocol: http
    hostname: 'react_demo.lndo.site'

# In local we use Lando.
drupal:
  db:
    database: drupal9
    username: drupal9
    password: drupal9
    host: database
    port: 3306
